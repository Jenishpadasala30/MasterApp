#! keycloak-keycloak_data-persistentVolumeClaim.yaml
# Generated code, do not edit
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: keycloak-keycloak-data
    namespace: portfolio-project
    labels:
        com.docker.compose.service: keycloak
        com.docker.compose.volume: keycloak_data
spec:
    accessModes:
        # TODO would need to check which services use this volume
        - ReadWriteOnce
    volumeMode: Filesystem
    resources:
        requests:
            storage: 100Mi
